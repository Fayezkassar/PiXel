<UserControl x:Class="ImageResizingApp.Views.TableListingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Margin="10 20 0 0" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Text="Pick a table column to resize"></TextBlock>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Tables}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button HorizontalAlignment="Stretch" Style="{StaticResource MaterialDesignPaperDarkButton}" 
                                    Content="{Binding Name}" 
                                    Margin="10,0,5,5"
                                    Command="{Binding DataContext.TableSelectedCommand,RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding Name}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            <TabControl Grid.Column="1" Style="{StaticResource MaterialDesignNavigatilRailTabControl}" TabStripPlacement="Top">
                <TabItem Header="Stats">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="50"/>
                            <RowDefinition Height="50"/>
                            <RowDefinition Height="50"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text='{Binding Path=TableStats.TableSize, StringFormat="Table Approx Size: {0}"}' />
                        <TextBlock Grid.Row="1" Text='{Binding Path=TableStats.RecordsNumber, StringFormat="Number of Records: {0}"}' />
                        <TextBlock Grid.Row="2" Text='{Binding Path=TableStats.RecordSize, StringFormat="Record Approx Size: {0}"}' />
                    </Grid>
                </TabItem>
                <TabItem Header="Columns">
                    <DataGrid VerticalAlignment="Top" ItemsSource="{Binding Path=Columns}" AutoGenerateColumns="False" CanUserResizeColumns="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Coumns Name" Binding="{Binding Name}"/>
                            <DataGridTextColumn Header="Column Type" Binding="{Binding ColumnType}"/>
                            <DataGridTextColumn Header="TOTAL SIZE" Binding="{Binding TotalSize}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>
                <TabItem Header="Data">
                    <DataGrid ItemsSource="{Binding Path=Data}" IsReadOnly="True"/>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</UserControl>
